# 📧 Настройка отправки уведомлений с Mail.ru (bk.ru)

## Настройка для cmsauto@bk.ru

### Конфигурация переменных окружения

#### Для Azure (User Secrets):

В Azure Portal → App Service → Configuration → Application settings добавьте:

```
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=cmsauto@bk.ru
bk=<пароль-от-почты-cmsauto@bk.ru>
```

**Важно:** Секрет с именем `bk` будет автоматически использован как `EMAIL_PASS`, если переменная `EMAIL_PASS` не задана.

#### Для локальной разработки (.env):

```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=cmsauto@bk.ru
EMAIL_PASS=ваш-пароль-от-cmsauto@bk.ru
```

Или используйте имя секрета напрямую:

```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=cmsauto@bk.ru
bk=ваш-пароль-от-cmsauto@bk.ru
```

#### Для Railway:

В разделе **Variables** добавьте:

```
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=cmsauto@bk.ru
bk=<пароль-от-почты>
```

#### Для Render:

В разделе **Environment Variables**:

```
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=cmsauto@bk.ru
bk=<пароль-от-почты>
```

## Получение пароля для Mail.ru

1. Войдите в почту **cmsauto@bk.ru**
2. Перейдите: [Настройки → Безопасность](https://e.mail.ru/settings/security)
3. Включите двухфакторную аутентификацию (если еще не включена)
4. Найдите раздел **"Пароли для внешних приложений"**
5. Создайте новый пароль для **"Почтовая программа"**
6. Скопируйте созданный пароль

## Проверка настройки

После настройки переменных окружения:

1. Перезапустите сервер
2. Создайте тестовый осмотр
3. Укажите email получателя в поле "Email исполнителя"
4. Проверьте папку "Входящие" получателя

Письма будут отправляться **с адреса cmsauto@bk.ru**.

## Особенности

- ✅ Автоматическое определение SSL для Mail.ru (порт 465)
- ✅ Поддержка пароля из User Secrets (секрет `bk`)
- ✅ Письма отправляются асинхронно (не блокируют создание осмотра)
- ✅ Логирование успешных отправок и ошибок

## Структура письма

Уведомления содержат:
- Номер осмотра
- Адрес осмотра
- Тип имущества
- Количество объектов
- Прямую ссылку на осмотр

---

**Готово!** После настройки все уведомления будут отправляться с адреса **cmsauto@bk.ru** на email, указанный при создании осмотра.

