# 📧 Настройка отправки email-уведомлений

## Обзор

Система автоматически отправляет email-уведомления на адрес исполнителя (`inspector_email`), указанный при создании осмотра. Уведомления отправляются:

- При создании нового осмотра
- При изменении статуса осмотра (Проверка, Готов, Доработка)

## Настройка email (Nodemailer)

### Шаг 1: Выбор email-провайдера

Система поддерживает любые SMTP-серверы. Примеры провайдеров:

- **Gmail** (рекомендуется для тестирования)
- **Yandex Mail**
- **Mail.ru**
- **Outlook/Hotmail**
- **Собственный SMTP-сервер**

### Шаг 2: Настройка Gmail (пример)

#### Для Gmail:

1. Включите двухфакторную аутентификацию в вашем Google аккаунте
2. Создайте пароль приложения:
   - Перейдите в [Управление аккаунтом Google](https://myaccount.google.com/)
   - Безопасность → Двухэтапная аутентификация → Пароли приложений
   - Выберите "Почта" и устройство, создайте пароль
   - Скопируйте созданный 16-символьный пароль

3. Настройте переменные окружения:

```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-character-app-password
```

### Шаг 3: Настройка Mail.ru (bk.ru, inbox.ru, list.ru, mail.ru)

```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=your-email@mail.ru
EMAIL_PASS=your-password
```

**Важно для Mail.ru:**
1. Используйте порт 465 с SSL (secure: true)
2. Включите двухфакторную аутентификацию в настройках почты
3. Создайте пароль для внешних приложений:
   - Зайдите в [Настройки Mail.ru](https://e.mail.ru/settings/security)
   - Безопасность → Пароли для внешних приложений
   - Создайте пароль для почтовых программ
   - Используйте этот пароль как `EMAIL_PASS`

**Пример для bk.ru, inbox.ru, list.ru:**
```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=your-email@bk.ru
EMAIL_PASS=your-app-password
```

**Поддержка User Secrets (Azure):**

Система автоматически поддерживает использование пароля из User Secrets. Если переменная `EMAIL_PASS` не задана, система попытается использовать секрет с именем `bk`:

```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=cmsauto@bk.ru
bk=<пароль-из-user-secrets>
```

**Пример для cmsauto@bk.ru:**
```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=cmsauto@bk.ru
EMAIL_PASS=<пароль-от-почты>
# Или используйте секрет:
bk=<пароль-из-user-secrets>
```

### Шаг 4: Настройка Yandex Mail

```env
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USER=your-email@yandex.ru
EMAIL_PASS=your-password
```

**Важно:** В `utils/notifications.js` для Yandex автоматически используется `secure: true` и `port: 465`.

### Шаг 5: Настройка переменных окружения

#### Для локальной разработки:

Создайте файл `.env` в корне проекта (рядом с `server.js`):

```env
# Email настройки
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# URL клиента (для ссылок в письмах)
CLIENT_URL=http://localhost:3000
```

#### Для деплоя на Railway:

1. Откройте проект в Railway
2. Перейдите на вкладку **Variables**
3. Добавьте следующие переменные:

```
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
CLIENT_URL=https://jfsagro-glitch.github.io/CMS_chek
```

#### Для деплоя на Render:

1. Откройте проект в Render Dashboard
2. Перейдите в раздел **Environment**
3. Добавьте переменные:

```
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
CLIENT_URL=https://jfsagro-glitch.github.io/CMS_chek
```

## Как это работает

### При создании осмотра:

1. Пользователь заполняет форму создания осмотра
2. В поле **"Email исполнителя"** (`inspector_email`) указывается email-адрес
3. После создания осмотра система:
   - Формирует HTML-письмо с деталями осмотра
   - Отправляет письмо на указанный email
   - Логирует результат в консоль

### Содержимое письма:

- Тема: "Новый осмотр №[номер]"
- Приветствие с именем исполнителя
- Детали осмотра:
  - Номер осмотра
  - Адрес
  - Тип имущества
  - Количество объектов
  - Комментарий (если есть)
- Кнопка "Перейти к осмотру" с прямой ссылкой

### При изменении статуса:

Email отправляется автоматически при изменении статуса на:
- **Проверка** - "Осмотр готов к проверке"
- **Готов** - "Осмотр принят"
- **Доработка** - "Осмотр возвращен на доработку"

## Проверка работы

### Тестирование локально:

1. Убедитесь, что переменные окружения настроены
2. Запустите сервер: `npm start`
3. Создайте тестовый осмотр через форму
4. Проверьте логи сервера:
   ```
   Email отправлен на: test@example.com
   ```
5. Проверьте папку "Входящие" и "Спам" указанного email

### Отладка:

Если email не отправляется, проверьте:

1. **Логи сервера:**
   ```bash
   # Проверьте наличие ошибок в консоли
   ```

2. **Переменные окружения:**
   ```bash
   # Убедитесь, что переменные установлены
   echo $EMAIL_USER
   ```

3. **Подключение к SMTP:**
   - Проверьте правильность хоста и порта
   - Для Gmail: убедитесь, что используется пароль приложения, а не обычный пароль
   - Проверьте, что двухфакторная аутентификация включена (для Gmail)

4. **Firewall/Безопасность:**
   - Убедитесь, что порт 587 (или 465) не заблокирован
   - Некоторые корпоративные сети блокируют SMTP

## Безопасность

⚠️ **Важно:**

- **НЕ коммитьте** файл `.env` в Git
- Используйте **пароли приложений** для Gmail (не основной пароль)
- Храните секреты в переменных окружения на сервере
- Для продакшена используйте специализированные сервисы (SendGrid, Mailgun, AWS SES)

## Альтернативные email-сервисы

### SendGrid (рекомендуется для продакшена):

```env
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USER=apikey
EMAIL_PASS=your-sendgrid-api-key
```

### Mailgun:

```env
EMAIL_HOST=smtp.mailgun.org
EMAIL_PORT=587
EMAIL_USER=postmaster@your-domain.mailgun.org
EMAIL_PASS=your-mailgun-password
```

### AWS SES:

```env
EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
EMAIL_PORT=587
EMAIL_USER=your-aws-access-key
EMAIL_PASS=your-aws-secret-key
```

## Примеры использования

### Проверка настроек:

```javascript
// Проверить, настроен ли email
if (process.env.EMAIL_USER) {
  console.log('Email настроен:', process.env.EMAIL_USER);
} else {
  console.log('Email не настроен');
}
```

### Ручная отправка тестового письма:

```javascript
const { sendEmail } = require('./utils/notifications');

sendEmail(
  'test@example.com',
  'Тестовое письмо',
  'Это тестовое сообщение',
  '<h1>Тестовое письмо</h1><p>Это тестовое сообщение</p>'
).then(() => console.log('Отправлено'))
  .catch(err => console.error('Ошибка:', err));
```

## Поддержка

При возникновении проблем:

1. Проверьте логи сервера
2. Убедитесь, что переменные окружения настроены
3. Проверьте, что email-адрес исполнителя указан при создании осмотра
4. Для Gmail: убедитесь, что используется пароль приложения

---

**Готово!** После настройки переменных окружения email-уведомления будут отправляться автоматически на адрес, указанный при создании осмотра.

